<h1>Click an Item to View Price</h1>
<hr />
<%= link_to 'Create a new Item', new_store_item_path(@store)%>
<ul>
  <% @items.each do |item| %>
    <li><%= link_to item.name, [@store, item] %></li>
  <% end %>
</ul>